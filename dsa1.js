// Write a JavaWrite a JavaScript function that takes a string as input and returns the character that appears the most frequently. If multiple characters have the same frequency, return the first one.
// Input:
// "javascript"
// Output:
// "a"

function Repeat(value){
    // let str = "javascript";
    for(let i = 0; i < value.length; i++){
        console.log(value[i]);
        
    }
}

let val = Repeat("javascript");
// console.log(val);